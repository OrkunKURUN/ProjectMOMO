<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.orkun.autocrashhistory">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoCrashHistory">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUp" android:label="addAccount"></activity>
        <activity android:name=".MainMenu" android:label="main"></activity>
        <activity android:name=".Settings" android:label="settings"></activity>
        <activity android:name=".AccountSettings" android:label="settings_account"></activity>
        <activity android:name=".AddRecord" android:label="record_adder"></activity>
        <activity android:name=".SearchRecord" android:label="record_searcher"></activity>
        <activity android:name=".EditRecord" android:label="record_editor"></activity>
    </application>

</manifest>